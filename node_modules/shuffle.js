'use strict';

exports.shuffle = function( array ){
  var m = array.length, t, i;

  while( m ){

    i = Math.floor( Math.random() * m-- );

    t = array[m]; // Set last element to t
    array[m] = array[i];
    array[i] = t;
  }

  return array;
};